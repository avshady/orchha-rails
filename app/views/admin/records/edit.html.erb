<h2 class="adm-page-title">Edit record #<%= @index + 1 %> in <%= @key %></h2>
<div class="adm-card">
  <%= render "form", record: @record, key: @key, url: admin_collection_record_path(@key, @index), method: :patch %>
</div>
