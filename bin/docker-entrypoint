#!/bin/bash -e

# If running the rails server then create or migrate existing database
# and seed content blocks from content.json (idempotent, never overwrites).
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare db:seed
fi

exec "${@}"
